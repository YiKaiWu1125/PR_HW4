import os
import random

def main():
    while True:
        #num_epochs = 1#random.randint(5, 30)
        #os.system(str("python main.py -epochs " + str(num_epochs) ))
        os.system(str("python big.py"))

if __name__ == "__main__":
    main()